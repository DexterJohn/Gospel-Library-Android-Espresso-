<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/themeStyleColorBackground"
    android:foreground="?attr/selectableItemBackground"
    android:minHeight="56dp"
    tools:layout_editor_absoluteX="0dp"
    tools:layout_editor_absoluteY="25dp">


    <ImageView
        android:id="@+id/coverArtImageView"
        android:layout_width="@dimen/catalog_list_artwork_width"
        android:layout_height="@dimen/catalog_list_artwork_height"
        android:layout_marginBottom="4dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="4dp"
        android:scaleType="centerCrop"
        android:src="@drawable/cover_default_list"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription"/>

    <View
        android:id="@+id/coverArtAlphaView"
        android:layout_width="@dimen/catalog_list_artwork_width"
        android:layout_height="@dimen/catalog_list_artwork_height"
        android:layout_marginBottom="4dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="4dp"
        android:alpha=".5"
        android:background="?attr/themeStyleColorListBackground"
        android:scaleType="centerCrop"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:visibility="visible"/>

    <ProgressBar
        android:id="@+id/installProgressBar"
        style="@style/AppTheme.Widget.ProgressBar.Horizontal"
        android:layout_width="@dimen/catalog_list_artwork_width"
        android:layout_height="4dp"
        android:layout_marginBottom="4dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/coverArtImageView"
        app:layout_constraintStart_toStartOf="@+id/coverArtImageView"
        tools:max="100"
        tools:progress="78"
        tools:visibility="visible"/>


    <ImageView
        android:id="@+id/overflowMenuView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="8dp"
        android:alpha="0.64"
        android:background="?attr/selectableItemBackground"
        android:padding="12dp"
        android:tint="?attr/themeStyleColorTextItemSubTitle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_lds_more_vert_24dp"
        tools:ignore="ContentDescription,RtlHardcoded"
        tools:visibility="visible"/>

    <TextView
        android:id="@+id/titleTextView"
        style="?attr/themeStyleWidgetText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:paddingEnd="8dp"
        android:paddingStart="0dp"
        android:textColor="?attr/themeStyleColorTextItemTitle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/overflowMenuView"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toEndOf="@+id/coverArtImageView"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="RtlHardcoded"
        tools:text="Book of Mormon "/>
</android.support.constraint.ConstraintLayout>